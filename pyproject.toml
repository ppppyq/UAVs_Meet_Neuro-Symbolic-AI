[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "uavs-meet-neuro-symbolic-ai"
version = "0.1.0"
description = "A lightweight literature-review engineering workspace for UAV neuro-symbolic AI."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = []

[tool.setuptools]
packages = []
