# Figure sources

Editable Mermaid drafts for the proposed review figures. They are not rendered
by the default build and do not require a graph-rendering dependency.

Mark these as `Proposed framework`; they are sketches, not results from an
experimental system.
